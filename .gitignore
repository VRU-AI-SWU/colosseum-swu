secrets/
*.seeds.yaml
agents/diamond/
.DS_Store
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# policy ที่เทรนเอง + checkpoint — สร้างใหม่ได้จาก examples/train_ppo.py
# ไม่ commit เพราะหนัก (หลักร้อย MB) และ Diamond baseline ตัวจริงต้องอยู่ที่ colosseum-hypogeum
envs/*/models/
envs/*/tb/

# ข้อมูลของ arena serve — ฐานข้อมูล SQLite + artifact ที่นิสิตอัพโหลด
.arena-dev/
*.db
*.db-wal
*.db-shm
