"""กล่องกันโค้ดนิสิต — ใช้ร่วมกันทุกชนิดโจทย์

    protocol.py   กรอบข้อความบน stdin/stdout + ตัวเข้ารหัส ndarray
    launcher.py   วิธีเปิด process ของนิสิต (subprocess ตอน dev · Docker ตอนตัดสิน)

**แพ็กเกจนี้ไม่รู้จักโจทย์ใดเลย** — ไม่รู้ว่ามี episode หรือมี DataFrame
สิ่งที่มันรู้คือ "เปิด process หนึ่งตัวในกล่อง แล้วคุยกับมันด้วยข้อความ"
ชื่อข้อความของแต่ละโจทย์อยู่ในแพ็กเกจของโจทย์นั้น (`agent_env/messages.py`
· `prediction/messages.py`) เพื่อให้เพิ่มโจทย์ชนิดที่สามได้โดยไม่ต้องแตะที่นี่

เดิมสองไฟล์นี้อยู่ใน `runners/agent_env/` เพราะตอนนั้นมีโจทย์ชนิดเดียว
ย้ายออกมาตอนเพิ่มโจทย์ทำนายของ CP462 — ทั้งคู่ไม่มีอะไรผูกกับ RL เลย
"""
