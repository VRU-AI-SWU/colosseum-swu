"""task template `prediction` — โจทย์ทำนายด้วยโมเดลที่เทรนมาแล้ว (CP462)

    plugin.py         สัญญาที่ env ของโจทย์ต้องทำตาม
    runner.py         ฝั่ง trusted — ถือเฉลย · ตรวจสามชั้น · ให้คะแนน
    predictor_host.py ฝั่ง untrusted — อยู่ใน container โหลด `predictor.py` ของนิสิต
    frame.py          ส่ง DataFrame ข้ามเส้นโดยชนิดข้อมูลไม่เพี้ยน (อยู่ในกล่องด้วย)
    messages.py       ชื่อข้อความเฉพาะของโจทย์ชนิดนี้ (อยู่ในกล่องด้วย)
    sandbox.py        image + host module ของโจทย์นี้ — **ฝั่ง trusted**

ต่างจาก `agent_env` ตรงที่ไม่มี episode และไม่มี seed ของฉาก — สิ่งที่เป็นความลับ
คือ **เฉลยของชุดที่ใช้ตัดสิน** ซึ่งไม่เคยเข้าไปในกล่องเลย เข้าไปแค่ฟีเจอร์

⚠️ **ไฟล์นี้ต้องไม่ import อะไรเลย** — ด้วยเหตุผลเดียวกับ `runners/agent_env/__init__.py`
แพ็กเกจนี้มีทั้งไฟล์ที่อยู่ในกล่องและไฟล์ฝั่ง trusted
"""
